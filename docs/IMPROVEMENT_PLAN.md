# Improvement Plan — Chat Ursa

**Дата:** 05.06.2026  
**Основание:** Полный аудит проекта (backend 5076 строк, frontend 9774 строк, 15+ файлов)

---

## 1. 🔧 Рефакторинг (самое важное)

| # | Что | Почему |
|---|---|---|
| 1.1 | **Разбить App.js на компоненты** | ✅ Выполнено: ChatList, ChatWindow, MessageInput, AdminPanel, CalendarTasks вынесены в отдельные файлы. App.js: 9323→7540 строк |
| 1.2 | **Разбить index.css** | ✅ Выполнено: разделён на 8 CSS-модулей в `src/css/` |
| 1.3 | **Очистить `backend/public/`** | ✅ Выполнено: добавлен `.gitignore` + `npm run clean` |
| 1.4 | **Перейти с sql.js на `better-sqlite3`** | ✅ Выполнено: WAL mode, параметризованные запросы, исправлены SQL-инъекции |

---

## 2. 🆕 Недостающий функционал

| # | Фича | Сложность | Описание |
|---|---|---|---|
| 2.1 | **Поиск по сообщениям** | Средняя | ✅ Выполнено: клиентский поиск по всем чатам с навигацией, встроен в сайдбар |
| 2.2 | **Индикатор печатания** | Средняя | ✅ Выполнено: `socket.emit('typing')`/`user_typing`, анимация точек в шапке чата, авто-сброс 3с |
| 2.3 | **Редактирование сообщений** | Средняя | ✅ Выполнено: контекстное меню → edit\_message event, обновление в БД в реальном времени |
| 2.4 | **Pinned messages** | Низкая | ✅ Выполнено: закрепление/открепление, бар в шапке, модалка со списком |
| 2.5 | **Badge непрочитанных** | Средняя | ✅ Выполнено: счётчик на каждом чате, сброс при открытии чата |
| 2.6 | **Push-уведомления** | Высокая | ✅ Выполнено: Service Worker (`sw.js`), Web Push API, подписка/отправка |
| 2.7 | **Drag & drop файлов** | Низкая | ✅ Выполнено: drop-зона над формой ввода, `handleDrop` + визуальный отклик |
| 2.8 | **Галерея изображений** | Средняя | ✅ Выполнено: модалка MediaViewer с сеткой изображений чата |
| 2.9 | **Темы (Dark mode)** | Средняя | ✅ Выполнено: CSS variables, `data-theme`, переключение, исправлены хардкодные цвета |

---

## 3. 🛡 Безопасность

| # | Что | Почему |
|---|---|---|
| 3.1 | **Параметризованные SQL-запросы** | ✅ Выполнено: исправлены при миграции на better-sqlite3 |
| 3.2 | **Ключ шифрования в env** | ✅ Выполнено: загрузка через dotenv, `.env.example`, warning при отсутствии |
| 3.3 | **CSRF защита** | ✅ Выполнено: CORS по whitelist, helmet с CSP, cookie-parser, `/api/csrf-token` |
| 3.4 | **2FA** | ✅ Выполнено: TOTP (speakeasy+QRCode), API generate/verify/disable, проверка при входе, UI в админ-панели |
| 3.5 | **Лимит на WebSocket события** | ✅ Выполнено: rate-limiter 30 событий/сек на сокет, очистка при disconnect |

---

## 4. 📱 Клиентские версии

| # | Что | Описание |
|---|---|---|
| 4.1 | **PWA (Progressive Web App)** | ✅ Выполнено: manifest.json, SVG иконки, meta-теги (theme-color, apple-mobile), Service Worker с кэшированием статики |
| 4.2 | **Адаптация под мобильные** | ✅ Выполнено: навигация переезжает вниз (tab bar), full-page модалки, адаптив календаря/телефонов/настроек/админки, touch-friendly, media queries 768px/480px |
| 4.3 | **Telegram-подобные треды** | ✅ Выполнено: replyCount на сообщениях, кнопка 💬 N, модалка треда с ответами, REST API `/api/thread/:messageId` + POST `/api/messages` |

---

## 5. 🚀 Инфраструктура

| # | Что | Описание |
|---|---|---|
| 5.1 | **Android (Capacitor)** | ✅ Выполнено: `android/` проект Capacitor, build → sync → open в Android Studio для сборки APK |
| 5.2 | **Docker Compose** | `docker-compose up` — поднимает backend + frontend + nginx |
| 5.3 | **CI/CD (GitHub Actions)** | Авто-тесты → сборка → релиз при пуше в main |
| 5.4 | **Авто-миграции БД** | Вместо try/catch `ALTER TABLE` — версионированные миграции |
| 5.5 | **Prometheus метрики** | `/api/metrics` — msg/min, active users, RPS |

---

## 6. 🤖 Умные функции

| # | Что | Описание |
|---|---|---|
| 6.1 | **AI-ассистент (LLM)** | Бот-помощник может отвечать через AnyScale/OpenAI API |
| 6.2 | **Авто-суммаризация чата** | «Что нового за день?» — AI пересказывает непрочитанное |
| 6.3 | **Smart replies** | Предполагаемые ответы над полем ввода |

---

## Рекомендуемый порядок выполнения

1. ✅ **Рефакторинг App.js** + **очистка public/** — дешёво, боль убирает сразу
2. ✅ **Поиск + typing indicator + drag & drop** — быстрые победы для UX
3. ✅ **Параметризованные SQL** + **ключ шифрования в env** — безопасность
4. ✅ **Pinned messages + dark mode + редактор сообщений**
5. ⬜ **Docker** — когда появится need
